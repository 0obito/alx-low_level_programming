#include <unistd.h>
#include "main.h"

/**
 * swap_int - Entry point
 *
 * Description: swaps the values for 2 integers
 *
 * @a: the first swap integer
 *
 * @b: the second swap integer
 */
void swap_int(int *a, int *b)
{
int temp;
temp = *a;
*a = *b;
*b = s;
}
